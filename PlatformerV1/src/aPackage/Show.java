package aPackage;

import java.awt.Graphics;

public interface Show {
	/**
	 * This method should contain how the {@code Show} is shown on the screen. 
	 * @param g The Graphics context this Show should be displayed on
	 */
	public void show(Graphics g);
}
